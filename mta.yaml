_schema-version: "3.2"
ID: amrgamalnorthwindapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: amrgamalnorthwindapp-destination-content
  type: com.sap.application.content
  requires:
  - name: amrgamalnorthwindapp-destination-service
    parameters:
      content-target: true
  - name: amrgamalnorthwindapp-repo-host
    parameters:
      service-key:
        name: amrgamalnorthwindapp-repo-host-key
  - name: amrgamalnorthwindapp-uaa
    parameters:
      service-key:
        name: amrgamalnorthwindapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: amrgamalnorthwindapp_html_repo_host
          ServiceInstanceName: amrgamalnorthwindapp-html5-service
          ServiceKeyName: amrgamalnorthwindapp-repo-host-key
          sap.cloud.service: amrgamalnorthwindapp
        - Authentication: OAuth2UserTokenExchange
          Name: amrgamalnorthwindapp_uaa
          ServiceInstanceName: amrgamalnorthwindapp-xsuaa-service
          ServiceKeyName: amrgamalnorthwindapp-uaa-key
          sap.cloud.service: amrgamalnorthwindapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: amrgamalnorthwindapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: amrgamalnorthwindapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - amrgamalnorthwindapp.zip
      name: amrgamalnorthwindapp
      target-path: resources/
- name: amrgamalnorthwindapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: amrgamalnorthwindapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: amrgamalnorthwindapp-destination-service
    service-plan: lite
- name: amrgamalnorthwindapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: amrgamalnorthwindapp-xsuaa-service
    service-plan: application
- name: amrgamalnorthwindapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: amrgamalnorthwindapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
